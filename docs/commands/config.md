# GFL Config 命令技术文档

## 概述

`gfl config` 命令用于查看当前 GFL 配置，显示配置项的值、来源和优先级，支持别名 `c`。

## 实现原理

### 1. 命令定义

```go
var configCmd = &cobra.Command{
    Use:     "config",
    Aliases: []string{"c"},
    Short:   "View current configuration",
    Long:    strings.GetString("config", "long"),
    Run: func(cmd *cobra.Command, args []string) {
        // 实现逻辑
    },
}
```

### 2. 执行流程

#### 步骤 1: 配置信息读取
```go
configInfo := utils.ReadConfigWithSources()
finalConfig := configInfo.FinalConfig
```

#### 步骤 2: 表格化显示配置
使用 `go-pretty/v6/table` 创建格式化的配置表格：
```go
t := table.NewWriter()
t.SetOutputMirror(os.Stdout)
t.SetTitle(strings.GetString("config", "title"))
t.SetStyle(table.StyleRounded)
t.Style().Options.SeparateRows = true
t.Style().Options.DrawBorder = true
```

#### 步骤 3: 配置来源分析
动态确定每个配置项的来源：
```go
getSource := func(field string) string {
    // 按优先级从高到低检查各个配置文件
    for i := len(configInfo.Sources) - 1; i >= 0; i-- {
        source := configInfo.Sources[i]
        if !source.Exists {
            continue
        }
        // 检查该配置项是否在当前源中定义
        // ...
    }
    return strings.GetString("config", "default_value")
}
```

#### 步骤 4: 配置源详情显示
显示所有配置文件的存在状态和路径。

## 常用参数含义

此命令不接受任何参数，自动显示所有配置信息。

## 配置显示格式

### 1. 配置表格
显示三个主要列：
- **配置项**: 配置参数的名称
- **最终值**: 配置的实际值（考虑优先级）
- **来源**: 该值的来源文件

### 2. 配置项说明

#### debug (调试模式)
- **类型**: `bool`
- **默认值**: `false`
- **说明**: 启用调试输出模式
- **用途**: 故障排除和详细日志

#### devBaseBranch (开发基础分支)
- **类型**: `string`
- **默认值**: `"develop"`
- **说明**: 功能分支的基础分支
- **用途**: `gfl start` 命令的基础分支

#### productionBranch (生产分支)
- **类型**: `string`
- **默认值**: `"main"`
- **说明**: 生产环境的主分支
- **用途**: `gfl hotfix` 和发布命令的基础分支

#### nickname (开发者昵称)
- **类型**: `string`
- **默认值**: `""`
- **说明**: 开发者的标识符
- **用途**: 分支命名中的开发者标识

#### featurePrefix (功能前缀)
- **类型**: `string`
- **默认值**: `"feature"`
- **说明**: 功能分支的前缀
- **用途**: `feature/aric/feature-name`

#### fixPrefix (修复前缀)
- **类型**: `string`
- **默认值**: `"fix"`
- **说明**: 修复分支的前缀
- **用途**: `fix/aric/bug-description`

#### hotfixPrefix (热修复前缀)
- **类型**: `string`
- **默认值**: `"hotfix"`
- **说明**: 热修复分支的前缀
- **用途**: `hotfix/aric/critical-fix`

## 配置优先级

配置按以下优先级加载（高到低）：

### 1. 自定义配置文件
- **路径**: `GFL_CONFIG_FILE` 环境变量指定
- **优先级**: 最高
- **用途**: 项目特定配置
- **颜色**: 红色

### 2. 本地配置文件
- **路径**: `.gfl.config.local.yml`
- **优先级**: 高
- **用途**: 开发者个人配置
- **颜色**: 黄色

### 3. 全局配置文件
- **路径**: `.gfl.config.yml`
- **优先级**: 中
- **用途**: 项目团队配置
- **颜色**: 蓝色

### 4. 默认配置
- **来源**: 程序内置
- **优先级**: 最低
- **用途**: 系统默认值
- **颜色**: 青色

## 使用场景

### 1. 检查配置状态
```bash
gfl config
# 查看当前所有配置和它们的来源
```

### 2. 调试配置问题
```bash
# 当命令行为异常时
gfl config
# 检查配置是否正确加载
```

### 3. 团队配置统一
```bash
# 确认团队成员使用相同的配置
gfl config
# 对比输出结果
```

### 4. 配置迁移
```bash
# 在升级 GFL 时检查配置兼容性
gfl config
# 确认所有配置项都正确识别
```

### 5. 使用别名
```bash
gfl c
# 快速查看配置
```

## 显示效果示例

### 配置表格输出
```
┌─────────────────────────────────────────────────────┬─────────────────┬─────────────────┐
│                      配置项                          │      最终值      │      来源       │
├─────────────────────────────────────────────────────┼─────────────────┼─────────────────┤
│                   调试模式                           │      false      │    默认值       │
│                 开发基础分支                         │     develop     │    全局配置     │
│                 生产分支                             │      main       │    本地配置     │
│                    昵称                             │      aric       │    本地配置     │
│                  功能前缀                           │     feature     │    全局配置     │
│                   修复前缀                           │       fix       │    全局配置     │
│                  热修复前缀                          │     hotfix      │    全局配置     │
└─────────────────────────────────────────────────────┴─────────────────┴─────────────────┘
```

### 配置源详情输出
```
配置文件来源:
  🌍 全局配置: .gfl.config.yml
  🏠 本地配置: .gfl.config.local.yml
  🎯 自定义配置: ~/.gfl.custom.yml
```

### 配置优先级说明
```
配置优先级 (从高到低):
  🎯 自定义配置文件 (GFL_CONFIG_FILE 环境变量)
  🏠 本地配置文件 (.gfl.config.local.yml)
  🌍 全局配置文件 (.gfl.config.yml)
  💾 系统默认配置
```

## 配置文件示例

### 全局配置文件 (.gfl.config.yml)
```yaml
debug: false
devBaseBranch: "develop"
productionBranch: "main"
nickname: ""
featurePrefix: "feature"
fixPrefix: "fix"
hotfixPrefix: "hotfix"
```

### 本地配置文件 (.gfl.config.local.yml)
```yaml
nickname: "aric"
debug: true
```

### 自定义配置文件
```yaml
devBaseBranch: "dev"
productionBranch: "master"
featurePrefix: "feat"
```

## 注意事项

### 1. 配置文件存在性
- 显示所有可能的配置源，无论文件是否存在
- 不存在的源在详情列表中不会显示

### 2. 配置值优先级
- 最终值是所有配置源合并的结果
- 高优先级配置会覆盖低优先级配置

### 3. 颜色编码
- 不同来源使用不同颜色标识
- 帮助快速识别配置来源

### 4. 环境变量支持
- `GFL_CONFIG_FILE` 环境变量可以指定自定义配置文件
- 支持绝对路径和相对路径

### 5. 配置验证
- 基本的配置值验证
- 无效配置会显示默认值

## 使用示例

### 基本使用
```bash
gfl config
# 显示完整的配置表格和来源信息
```

### 使用别名
```bash
gfl c
# 等同于 gfl config
```

### 调试输出示例
```bash
$ gfl config

┌─────────────────────────────────────────────────────┬─────────────────┬─────────────────┐
│                      配置项                          │      最终值      │      来源       │
├─────────────────────────────────────────────────────┼─────────────────┼─────────────────┤
│                   调试模式                           │      true       │    本地配置     │
│                 开发基础分支                         │     develop     │    全局配置     │
│                 生产分支                             │      main       │    全局配置     │
│                    昵称                             │      aric       │    本地配置     │
│                  功能前缀                           │     feature     │    全局配置     │
│                   修复前缀                           │       fix       │    全局配置     │
│                  热修复前缀                          │     hotfix      │    全局配置     │
└─────────────────────────────────────────────────────┴─────────────────┴─────────────────┘

配置文件来源:
  🌍 全局配置: .gfl.config.yml
  🏠 本地配置: .gfl.config.local.yml

配置优先级 (从高到低):
  🎯 自定义配置文件 (GFL_CONFIG_FILE 环境变量)
  🏠 本地配置文件 (.gfl.config.local.yml)
  🌍 全局配置文件 (.gfl.config.yml)
  💾 系统默认配置
```

## 故障排除

### 常见问题及解决方案

1. **"配置未生效"**
   ```bash
   # 检查配置文件是否存在
   ls -la .gfl.config*

   # 检查配置语法
   yamllint .gfl.config.yml

   # 查看配置加载情况
   gfl config
   ```

2. **"配置来源不正确"**
   ```bash
   # 检查环境变量
   echo $GFL_CONFIG_FILE

   # 重置环境变量
   unset GFL_CONFIG_FILE
   gfl config
   ```

3. **"配置值显示为默认值"**
   - 配置文件可能不存在
   - 配置文件语法错误
   - 配置项名称错误

4. **"颜色显示异常"**
   - 终端不支持颜色
   - 设置 `NO_COLOR` 环境变量
   - 使用 `--no-color` 标志（如果支持）

## 高级用法

### 1. 配置文件调试
```bash
# 查看配置文件内容
cat .gfl.config.yml
cat .gfl.config.local.yml

# 验证 YAML 语法
python -c "import yaml; yaml.safe_load(open('.gfl.config.yml'))"
```

### 2. 配置备份和恢复
```bash
# 备份配置
cp .gfl.config.yml .gfl.config.yml.backup
cp .gfl.config.local.yml .gfl.config.local.yml.backup

# 恢复配置
cp .gfl.config.yml.backup .gfl.config.yml
```

### 3. 配置比较
```bash
# 比较不同环境的配置
diff .gfl.config.yml .gfl.config.prod.yml

# 使用 git 跟踪配置变化
git diff .gfl.config.yml
```

## 最佳实践

### 1. 配置管理策略
- 全局配置用于团队标准
- 本地配置用于个人定制
- 自定义配置用于特殊环境

### 2. 配置文件组织
- 将全局配置提交到版本控制
- 将本地配置添加到 .gitignore
- 为不同环境创建不同的自定义配置

### 3. 配置文档化
- 在项目文档中说明重要配置项
- 记录配置变更历史
- 提供配置示例文件

## 相关命令

- `gfl init`: 初始化配置文件
- `gfl start`: 使用配置创建分支
- `gfl hotfix`: 使用配置创建热修复分支
- `git config`: Git 原生配置命令

## 配置依赖

- 无特殊依赖
- 依赖文件系统读取配置文件
- 依赖环境变量获取自定义配置路径

## 扩展功能建议

### 1. 配置验证
```bash
# 验证配置完整性
gfl config --validate

# 检查配置兼容性
gfl config --check-compatibility
```

### 2. 配置导出
```bash
# 导出当前配置
gfl config --export > my-config.yml

# 导出为环境变量
gfl config --export-env
```

### 3. 配置比较
```bash
# 比较配置差异
gfl config --diff other-config.yml

# 比较默认配置
gfl config --diff-default
```